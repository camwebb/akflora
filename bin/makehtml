#!/bin/sh

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR/../pages_md

for i in *.md
do
    echo "  "${i/md/html}
    pandoc -s --template=template.html -o ../alaskaflora.org/pages/${i/md/html} $i
done

